// Stub main window — the wallet screens land with the UI todo.
export component MainWindow inherits Window {
    preferred-width: 480px;
    preferred-height: 800px;
    background: #10141a;
    Text {
        text: "micronuts wallet";
        color: #e8eaed;
        font-size: 22px;
    }
}
